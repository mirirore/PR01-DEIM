using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio2b : MonoBehaviour
{
    int n;
    int x;
    // Start is called before the first frame update
    void Start()
    {
        /* while
        n = 0;

        while (n < 10)
        {
          print(n);
          n++;
        }
        */


        //for con concatenación
        for (int x = 0; x < 10; x++)
        {
            print("x vale:" + x);
        }

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
